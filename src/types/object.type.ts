//With type inference no error is thrown
let person = {
    name: "chizaa",
    age: 25,
  };
  
  let car: { color: string; brand: string } = {
      color: 'red',
      brand: 'bmw',
  };
  
  
  let article: { author: string; content: string; article: string; image?: string} = {
      author: "chizaa",
      content: "web 3",
      article: "solidity",
  
  }
